import 'package:flutter/material.dart';

class BarretConnectionScreen extends StatelessWidget {
  const BarretConnectionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}