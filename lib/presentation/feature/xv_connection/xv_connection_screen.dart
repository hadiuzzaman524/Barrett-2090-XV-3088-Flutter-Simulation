import 'package:flutter/material.dart';

class XvConnectionScreen extends StatelessWidget {
  const XvConnectionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}